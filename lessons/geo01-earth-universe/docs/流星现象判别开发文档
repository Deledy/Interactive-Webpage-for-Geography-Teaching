你是一名资深前端工程师，请帮我开发一个用于天体教学的网页动画演示项目。

项目目标：
1. 用网页演示“流星体、流星、陨石”三者的区别。
2. 场景分为三层：
   - 太空层：大量小天体在缓慢漂浮，表示流星体。
   - 大气层：部分天体进入大气后速度加快，轨迹变亮并拉出尾迹，表示流星。
   - 地面层：少量未完全烧蚀的天体坠落到地面，落地后停留并显示为陨石。
3. 页面用于教学展示，要求轻量、流畅、可在普通电脑浏览器中运行。
4. 后续需要方便分发，所以请尽量使用原生 HTML、CSS、JavaScript，不要使用过重的 3D 引擎。

技术要求：
1. 使用 Canvas 2D 实现主要动画，不使用 DOM 大量生成粒子。
2. 使用 GSAP 控制场景切换、时间线动画和阶段过渡。
3. 代码结构清晰，尽量模块化，便于后续扩展。
4. 支持以下交互：
   - 开始演示
   - 暂停/继续
   - 重播
   - 切换教学阶段
5. 视觉风格：
   - 背景为宇宙/夜空风格
   - 粒子有轻微漂移和随机运动
   - 流星要有发光拖尾
   - 落地时有简单冲击光效或尘土效果
6. 需要适配不同屏幕尺寸，Canvas 要响应式缩放。
7. 请把代码拆分成多个文件，建议包括：
   - index.html
   - style.css
   - main.js
   - scene.js
   - particle.js
   - meteor.js
   - utils.js

实现要求：
1. 先给出项目目录结构。
2. 再给出完整可运行代码。
3. 代码中请写清楚注释，特别是：
   - 流星体如何漂浮
   - 流星如何进入大气层并加速
   - 陨石如何落地
4. 如果需要资源文件，请尽量使用程序绘制，不要依赖外部图片。
5. 请优先保证效果自然、性能轻量、代码易维护。

如果你认为有必要，可以先输出一个最小可运行版本，再在此基础上逐步增强。
