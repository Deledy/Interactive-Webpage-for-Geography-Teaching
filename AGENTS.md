# AGENTS.md

## 项目目标
本项目用于将初高中地理课程内容，转化为可运行的 HTML 教学网页。
目标是围绕教学目标、学生认知和课堂使用场景，产出清晰、可执行、可复用的网页方案。

## 执行原则
1. 始终以教学目标优先，不为炫技增加复杂度。
2. 优先使用原生 HTML / CSS / JavaScript，必要时再引入外部库。
3. 页面设计必须服务于知识理解、课堂讲解和学生操作。
4. 技术实现必须兼顾稳定性、可维护性、可复用性和移动端适配。
5. 所有输出尽量结构化，便于后续开发和交接。
6. 如果输入信息不足，先补关键缺失项，再继续推进。
7. 不要默认把输入内容视为同一层级；要区分事实、教学意图、呈现设想和待确认问题。
8. 角色分工要清晰：
   - 教育教学设计专家：负责教学结构与教学价值判断
   - 页面设计专家：负责页面布局、视觉层次与学习体验
   - 技术实现专家：负责技术可行性、库选择与实现路径
   - 总控专家：负责协调、裁剪、决策和范围控制
   - 开发文档专家：负责把方案整理成 AI 编程工具可执行的说明书

## 文件约定
1. `agents/current_task.md` 是当前任务输入模板，以它为准。
2. `agents/prompts/` 目录存放各角色提示词。
3. `agents/templates/` 目录存放交接模板。
4. `scripts/` 目录存放工程脚本（多课构建、结构校验等纯工程操作，不调用 AI API；Agent 编排由项目外 Agent 应用负责，不在项目本地维护）。
5. `agents/output/` 目录存放各阶段输出结果。
6. `lessons/` 目录按课题保存单节课项目文件（Vite 工程，每课独立开发、独立构建），统一结构为：`lesson.json`、`index.html`（Vite 入口）、`src/`（`main.ts` 入口 + `styles/` `data/` `utils/` `modules/` 分层）、`assets/`、`public/`（视频/3D 等大资源，构建时原样复制、不内联）、`docs/`、`dist/`（构建产物，禁止手改）。
7. `books/` 目录用于将单课汇编成整书（必修一 / 必修二），通过 `lesson.json` 自动汇总。
8. `shared/` 目录存放多课复用的公共组件、样式、工具和资源。
9. 单课开发文档统一放在该课 `docs/` 下，命名为 `01_教学设计交接包.md` ~ `05_开发文档.md`，另设 `06_资源引用.md` 作为该课资源引用登记表（外部来源资源的版权登记页面）。
10. 工程构建由根目录 `package.json` + `vite.config.ts` 驱动：`npm run dev` 开发、`npm run build` 单课构建（先类型检查）、`npm run build:all` 多课构建、`npm run check` 结构校验、`npm run test` 功能测试（vitest + jsdom）、`npm run preview` 预览产物。
11. 第三方库优先 npm 引入（如 Three.js），不再手工维护本地 `libs/`。
12. 所有外部来源资源（图片/视频/音频/字体等，非本课原创）必须登记到本课 `docs/06_资源引用.md`，写明作者/版权方、许可类型与来源链接，保持引用格式统一、可追溯；`npm run check` 会校验该文件是否存在。

## 推荐工作流
1. 读取 `agents/current_task.md`
2. 先进入教学设计
3. 再进入页面设计
4. 再进入技术实现
5. 再由总控审查冲突与范围
6. 最后生成开发文档
7. 将开发文档交给 AI 编程工具实现

## 输出要求
1. 输出必须尽量清晰、简洁、可执行。
2. 不要重复用户原话，要提炼成结论。
3. 要明确区分：
   - 必须实现
   - 建议实现
   - 可选实现
   - 不建议实现
4. 要说明风险、降级方案和待确认事项。
5. 要保证最终结果能够直接用于开发。

## 处理输入的规则
1. 用户输入可以较乱，允许混合知识点、教学想法、页面设想和疑问。
2. 先对输入进行分类整理，再进行专业判断。
3. 教学目标优先于页面表现，页面表现优先于技术偏好。
4. 如果某个呈现设想与教学目标冲突，应明确指出，并给出替代建议。
5. 如果某项内容不适合当前学段或成本过高，应建议降级或删减。

## 默认技术偏好
1. 优先单文件或轻量静态页面。
2. 优先复用现成组件思路。
3. 需要图表时优先简单库。
4. 需要地图时优先轻量方案。
5. 不优先引入复杂前端框架。

## 任务边界
1. 不要擅自扩展无关功能。
2. 不要为了完整性加入过多非必要模块。
3. 不要跳过中间交接步骤。
4. 不要把未确认内容当成定稿。

## 初始化
请始终以当前任务为准，围绕教学网页生产流程，输出结构化、可执行、可交接的结果。
