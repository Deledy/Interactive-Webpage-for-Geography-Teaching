# 教学网页开发步骤说明

本说明面向"开发一节课"的完整流程：**规划 → 开发 → 放资源 → 自检 → 分发**。每节课遵循同样的步骤，参考当前已完成课程 `lessons/geo01-earth-universe/`。

---

## 一、开发一节新课（7 步）

### 第 1 步：新建课程目录

在 `lessons/` 下创建目录，命名规则：`geoNN-英文短名`（`NN` 为册内序号，如 `geo02` = 必修一第 2 课），中文标题放在 `lesson.json` 里，目录名保持 ASCII（避免中文路径在打包/托管时的编码风险）。

```
lessons/geo02-xxx/
```

### 第 2 步：填写任务需求

在根目录 `agents/current_task.md` 中填写本节需求（知识点、教学想法、页面设想、疑问，可以写乱，之后由 Agent 整理）。这是工作流的起点。

### 第 3 步：多 Agent 生成开发文档

让项目外的 Agent 应用依次扮演 5 个角色（提示词在 `agents/prompts/`），产物写入 `lessons/<课>/docs/`：

| 顺序 | 角色 | 产物 |
| --- | --- | --- |
| 1 | 教育教学设计专家 | `docs/01_教学设计交接包.md` |
| 2 | 页面设计专家 | `docs/02_页面设计交接包.md` |
| 3 | 技术实现专家 | `docs/03_技术实现交接包.md` |
| 4 | 总控专家 | `docs/04_总控摘要.md` |
| 5 | 开发文档专家 | `docs/05_开发文档.md` |

**边界：Agent 只生成开发报告，不写代码。**

### 第 4 步：AI 编程工具实现代码

将 `docs/05_开发文档.md` 交给会写代码的 AI，按 `agents/templates/开发模板指令.md` 的规则实现。课程目录结构必须遵循：

```
lessons/geo02-xxx/
├── lesson.json    # 课程登记表（见第 5 步）
├── index.html     # Vite 入口
├── src/           # 代码
│   ├── main.ts    # 入口（按顺序初始化各功能模块）
│   ├── styles/    # 样式
│   ├── data/      # 课程数据
│   ├── utils/     # 工具函数
│   ├── modules/   # 功能模块
│   └── types.ts
├── assets/        # 本课资源（图片/图标/字体，见"资源放置"）
├── public/        # 大资源外置（视频/3D 模型，见"资源放置"）
├── docs/          # 上面 5 份开发文档
└── dist/          # 构建产物（禁止手改）
```

### 第 5 步：登记 lesson.json

实现完成后填写本课 `lesson.json`，字段示例：

```json
{
  "book": "必修一",
  "chapter": "第二章",
  "order": 2,
  "lessonTitle": "课程中文标题",
  "grade": "高中必修",
  "status": "done",
  "engine": "vite",
  "entry": "dist/index.html",
  "resources": [
    { "path": "assets/images/xxx.jpg", "type": "image" }
  ],
  "tags": ["标签1", "标签2"],
  "description": "本课内容简介"
}
```

要点：
- `status`：开发完成并验证通过后填 `done`（`check` 会要求 `done` 的课存在构建产物）。
- `resources`：**登记本课引用的全部资源**（`path` 相对本课目录 + `type`），供 `npm run check` 校验完整性、供分发清单使用。
- `order`：册内顺序，`books/` 整书汇编按它排序。

### 第 6 步：自检（3 条命令）

| 命令 | 作用 | 通过标准 |
| --- | --- | --- |
| `npm run check` | 结构体检 | 无错误（远程 URL 残留等仅告警，不阻塞） |
| `npm test` | 功能测试 | 全部用例通过 |
| `npm run build:all` | 正式构建 | 构建成功，dist/ 产物正常 |

### 第 7 步：分发（见"三、分发方式"）

---

## 二、资源文件放哪里

**规则一句话：能内联的放 `assets/`，太大不能内联的放 `public/`，多课共用的放 `shared/`。**

### 1. `assets/` —— 图片、图标、字体（会内联进 HTML）

```
assets/
├── images/    # 课程用图（jpg/png/svg）
├── icons/     # 图标
└── fonts/     # 字体（若需要）
```

- 构建时这些资源会被 base64 **内联进 index.html**，保证最终分发物是单个 HTML、双击可用。
- 命名：snake_case（如 `celestial-nebula.jpg`）。
- 引用方式：相对路径，如 `assets/images/celestial-nebula.jpg`。
- **禁止使用远程 URL**（保证离线可用），也不得引用本项目以外的在线图片。

### 2. `public/` —— 视频、3D 模型等大资源（原样复制、不内联）

```
public/
├── videos/    # 视频（mp4）
└── models/    # 3D 模型（glb 等）
```

- `public/` 下文件**不参与内联**，构建时原样复制到 `dist/` 对应路径。
- 引用方式：以 `/` 开头，如 `/assets/videos/xxx.mp4`（构建后自动转为相对路径）。
- 为什么分开：视频若内联会变成超大 HTML（base64 膨胀 33%），所以外置；`<video>` 标签在双击打开（file://）时**可以正常播放**本地视频。
- **3D 模型的例外**：three.js 加载模型走 fetch，双击打开时会被浏览器安全拦截，3D 课分发时需配一键启动器或在线部署（见"三"）。

### 3. `shared/` —— 多课复用的公共组件/样式/资源

可被多课复用的组件、样式、图片放根目录 `shared/`，开发时按需引入/复制进单课。

### 4. 无论放哪，都要登记进 lesson.json 的 `resources`

这样 `npm run check` 才能校验资源是否齐全、分发时才知道要带上哪些文件。

---

## 三、分发方式

### 单课分发

```powershell
npm run build
```

产物：`lessons/<课>/dist/index.html` —— **JS/CSS/图片/字体全部内联的单个 HTML 文件**，双击即可在浏览器打开，无需服务器。构建后同时自动生成 `dist/<课>.zip`。

- 把 `dist/index.html`（或整个 zip）发给老师即可。
- 有视频的课：产物是 `index.html + 视频文件`，**整个文件夹一起**拷给对方（双击 index.html，视频可正常播放）。
- 3D 课：需配一键启动器（自动起本地服务器打开页面）或部署到在线托管。

### 整书分发

```powershell
npm run deploy
```

产物：根目录 `dist-book/`，结构为总览页 `index.html` → 书目录 `必修一/index.html` → 各课单页。整个文件夹上传任意静态托管（如 GitHub Pages）即可，或拷 U 盘双击 `index.html`。

### 常用命令速查

| 命令 | 作用 |
| --- | --- |
| `npm run dev` | 开发服务器（默认打开唯一课程；多课时先设 `$env:VITE_LESSON='geo02-xxx'`） |
| `npm run build` | 构建单课（默认唯一课程；多课时配合 `VITE_LESSON`） |
| `npm run build:all` | 构建全部课程 |
| `npm run build:lesson -- --lesson=geo02-xxx` | 构建指定课程 |
| `npm run check` | 结构校验 |
| `npm test` | 功能测试 |
| `npm run assemble` | 按 lesson.json 重建整书清单与书首页 |
| `npm run deploy` | 生成可上线的 dist-book/ |
| `npm run preview` | 本地预览构建产物 |

---

## 四、目录结构速览

```
教学互动网页开发工作流/
├── AGENTS.md            # 总工作流规则（工具自动加载）
├── agents/              # Agent 工作流：prompts/ 提示词、templates/ 模板、current_task.md 任务输入、output/ 产出
├── scripts/             # 工程脚本（构建/校验/汇总/部署，纯工程操作）
├── lessons/             # 单课工程目录（每课独立开发）
├── books/               # 整书汇编（由脚本自动生成，勿手改）
├── shared/              # 多课复用组件/样式/资源
└── tests/               # 功能测试
```

---

## 五、常见问题

**Q：为什么双击 index.html 页面空白？**
A：浏览器禁止 file:// 加载"外链模块脚本"。本项目的构建会把 JS/CSS 全部内联，所以正常产物双击可用；若页面空白，请确认用的是构建后的 `dist/index.html`（而非源码 `index.html`），且没有手动改动过构建配置。

**Q：新增图片后要不要做什么？**
A：图片放入 `assets/images/` → 在 HTML/代码中以相对路径引用 → 登记到 `lesson.json` 的 `resources`。构建时自动内联。

**Q：多课共用的东西放哪？**
A：放 `shared/`；单课需要时引入/复制到本课，避免重复维护。

**Q：一节课开发到一半想换题目？**
A：改 `agents/current_task.md` 重新走 Agent 流程即可；已生成的 docs/ 保留作历史，必要时更新 lesson.json 的 `title`/`description`。
